<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="ATAG.Costing.WinUI.BuncherLivePreview"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel Spacing="14">
        <Border Style="{StaticResource ModuleBackgroundCardStyle}">
            <StackPanel Spacing="8">
                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Spacing="2">
                        <TextBlock FontWeight="SemiBold" Text="Selected buncher lay" />
                        <TextBlock Style="{StaticResource ModuleSupportingTextStyle}" Text="{Binding CoreLayoutDisplay}" TextWrapping="Wrap" />
                    </StackPanel>
                    <TextBlock Grid.Column="1" Foreground="{StaticResource ModuleResultBorderBrush}" Text="{Binding SelectedChoice.LayDisplay}" />
                </Grid>
                <ComboBox
                    DisplayMemberPath="Display"
                    Header="Core group to preview · visual only"
                    HorizontalAlignment="Stretch"
                    ItemsSource="{Binding CoreLayouts}"
                    SelectedItem="{Binding SelectedCoreLayout, Mode=TwoWay}" />
                <TextBlock Style="{StaticResource ModuleMetricLabelStyle}" Text="END VIEW · ACTUAL CORE GROUP" />
                <Canvas x:Name="CrossSectionCanvas" Height="190" SizeChanged="PreviewCanvas_SizeChanged" />
                <TextBlock Style="{StaticResource ModuleMetricLabelStyle}" Text="SIDE VIEW · LAY REPEAT" />
                <Canvas x:Name="SideProfileCanvas" Height="140" SizeChanged="PreviewCanvas_SizeChanged" />
                <TextBlock Style="{StaticResource ModuleSupportingTextStyle}" Text="{Binding TraceDisplay}" TextWrapping="Wrap" />
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
