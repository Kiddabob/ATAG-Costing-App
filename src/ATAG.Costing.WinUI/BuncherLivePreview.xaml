<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="ATAG.Costing.WinUI.BuncherLivePreview"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel Spacing="14">
        <InfoBar
            Message="The preview reads the selected retained lay. It does not infer or recommend a machine setting."
            Severity="Informational"
            Style="{StaticResource ModuleStatusInfoBarStyle}"
            Title="Visual check" />
        <Border Style="{StaticResource ModuleBackgroundCardStyle}">
            <StackPanel Spacing="8">
                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock FontWeight="SemiBold" Text="Selected buncher lay" />
                    <TextBlock Grid.Column="1" Foreground="{StaticResource ModuleResultBorderBrush}" Text="{Binding SelectedChoice.LayDisplay}" />
                </Grid>
                <Canvas x:Name="PreviewCanvas" Height="180" SizeChanged="PreviewCanvas_SizeChanged" />
                <TextBlock Style="{StaticResource ModuleSupportingTextStyle}" Text="{Binding TraceDisplay}" TextWrapping="Wrap" />
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
